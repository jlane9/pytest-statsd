============
Installation
============

Install through pip:

.. code-block:: bash

    pip install pytest-statsd


Install from source:

.. code-block:: bash

    cd /path/to/source/pytest-statsd
    python setup.py install